<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="12dp"
    android:background="@drawable/widget_player_bg">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/widget_artwork"
            android:layout_width="68dp"
            android:layout_height="68dp"
            android:layout_marginEnd="12dp"
            android:background="@drawable/widget_artwork_bg"
            android:clipToOutline="true"
            android:contentDescription="Portada"
            android:padding="1dp"
            android:scaleType="centerCrop"
            android:src="@drawable/ic_media_placeholder" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/widget_title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:maxLines="1"
                android:text="Nexora Player"
                android:textColor="#FFFFFFFF"
                android:textSize="15sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/widget_subtitle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:ellipsize="end"
                android:maxLines="1"
                android:text="Listo para reproducir"
                android:textColor="#CFFFFFFF"
                android:textSize="12sp" />

            <TextView
                android:id="@+id/widget_lyric"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:ellipsize="end"
                android:maxLines="2"
                android:text="Letra original no guardada"
                android:textColor="#F2FFFFFF"
                android:textSize="13sp"
                android:textStyle="bold" />
        </LinearLayout>
    </LinearLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="46dp"
        android:gravity="center"
        android:orientation="horizontal"
        android:paddingTop="8dp">

        <ImageButton
            android:id="@+id/widget_previous"
            android:layout_width="0dp"
            android:layout_height="38dp"
            android:layout_weight="1"
            android:background="@drawable/widget_control_bg"
            android:contentDescription="Anterior"
            android:padding="9dp"
            android:scaleType="center"
            android:src="@drawable/ic_notification_previous" />

        <ImageButton
            android:id="@+id/widget_play_pause"
            android:layout_width="0dp"
            android:layout_height="42dp"
            android:layout_marginStart="7dp"
            android:layout_weight="1"
            android:background="@drawable/widget_control_primary_bg"
            android:contentDescription="Reproducir o pausar"
            android:padding="8dp"
            android:scaleType="center"
            android:src="@drawable/ic_notification_playback" />

        <ImageButton
            android:id="@+id/widget_next"
            android:layout_width="0dp"
            android:layout_height="38dp"
            android:layout_marginStart="7dp"
            android:layout_weight="1"
            android:background="@drawable/widget_control_bg"
            android:contentDescription="Siguiente"
            android:padding="9dp"
            android:scaleType="center"
            android:src="@drawable/ic_notification_next" />

        <ImageButton
            android:id="@+id/widget_favorite"
            android:layout_width="0dp"
            android:layout_height="38dp"
            android:layout_marginStart="7dp"
            android:layout_weight="1"
            android:background="@drawable/widget_control_bg"
            android:contentDescription="Favorito"
            android:padding="9dp"
            android:scaleType="center"
            android:src="@drawable/ic_notification_favorite_outline" />
    </LinearLayout>
</LinearLayout>
